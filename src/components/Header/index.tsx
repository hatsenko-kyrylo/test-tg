import React from 'react';
import WalletButton from '../WalletButton';

const Header = () => {
    return (
        <div>
            <WalletButton />
        </div>
    );
};

export default Header;
